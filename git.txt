git 명령어

연동하기
git remote add origin 레파지토리 주소

git config --global user.name "pakurrr"
git config --global user.email "이메일"

git push origin master (저장)



연동해제
git remote remove <repository name>

원격저장소 연결 끊기
git remote remove origin

.git 파일 삭제
rm -rf .git

변경된 파일들 모두 트랙킹
git add . 

커멧 메시지 설정과 함 께 commit (-m 이 message의 약어같다.)
git commit -m "커밋메세지" 